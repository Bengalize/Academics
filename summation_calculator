# Summation Calculator: Calculate a specific summation formula

def kalkul(n):
    somme = 0
    for i in range(1, n + 1):
        somme += (i / (i + 1) + (n + 1) / (n + 2))
    return somme

if __name__ == "__main__":
    print(kalkul(4))
